<UserControl x:Class="WPFSmallWorld.Game"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="525">

    <StackPanel>

        <ScrollViewer HorizontalScrollBarVisibility="Auto">
            <Grid Background="Azure">
                <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Name="mapGrid">

                </Grid>
            </Grid>
        </ScrollViewer >

        <StackPanel Orientation="Horizontal" Height="auto" Name="unitSelecter">

        </StackPanel>

        <Grid Background="White">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel HorizontalAlignment="Stretch" Grid.Column="0" Margin="2">
                <TextBlock FontWeight="Bold" FontSize="14">Peuple 1</TextBlock>
                <TextBlock>Points :</TextBlock>
                <TextBlock>Unités :</TextBlock>
            </StackPanel>

            <StackPanel HorizontalAlignment="Stretch" Grid.Column="1" Margin="2">
                <TextBlock FontWeight="Bold" FontSize="14">Peuple 2</TextBlock>
                <TextBlock>Points :</TextBlock>
                <TextBlock>Unités :</TextBlock>
            </StackPanel>

            <StackPanel HorizontalAlignment="Stretch" Grid.Column="2" Margin="2">
                <Button x:Name="finDeTour" Content="Fin de Tour" Click="Button_Click" HorizontalAlignment="Center"/>
                <TextBlock><Run Text="Tours restants :"/></TextBlock>
            </StackPanel>
        </Grid>
    </StackPanel>
</UserControl>
