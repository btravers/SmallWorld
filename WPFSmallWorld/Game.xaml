<UserControl x:Class="WPFSmallWorld.Game"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

        <StackPanel>
            <Grid Background="Azure" VerticalAlignment="Stretch">
            </Grid>

            <Grid Background="Azure">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel Background="Blue" HorizontalAlignment="Stretch" Grid.Column="0">
                    <TextBlock FontWeight="Bold" FontSize="14">Peuple 1</TextBlock>
                    <TextBlock>points</TextBlock>
                    <TextBlock>unités restantes</TextBlock>
                </StackPanel>

                <StackPanel Background="Red" HorizontalAlignment="Stretch" Grid.Column="1">
                    <TextBlock FontWeight="Bold" FontSize="14">Peuple 2</TextBlock>
                    <TextBlock>points</TextBlock>
                    <TextBlock>unités restantes</TextBlock>
                </StackPanel>

                <StackPanel Background="Green" HorizontalAlignment="Stretch" Grid.Column="2">
                    <Button x:Name="finDeTour" Content="Fin de Tour" Click="Button_Click" HorizontalAlignment="Center"/>
                    <TextBlock><Run Text="tours avant fin"/></TextBlock>
                </StackPanel>
            </Grid>
        </StackPanel>
</UserControl>
