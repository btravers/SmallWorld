<Window x:Class="WPFSmallWorld.MainWindow"
        x:Name="GlobalWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:local="clr-namespace:WPFSmallWorld"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Small World" Height="350" Width="525">
    
    <Window.Resources>
        <Style TargetType="Rectangle" x:Key="selectionStyle">
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Stroke" Value="Blue"/>
            <Setter Property="Panel.ZIndex" Value="333"/>
            <Setter Property="Visibility" Value="Collapsed"/>
        </Style>                   
    </Window.Resources>
    
    <Grid>
        <local:Select x:Name="SelectScreen"/>
        <local:Game x:Name="GameScreen" Visibility="Collapsed"/>
    </Grid>
</Window>
